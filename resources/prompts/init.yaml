id: init
description: Initialize project instructions and defaults.
template: |
  Initialize project guidance for this repository.
  Consider setup, conventions, commands, and local workflow.
  Extra instruction: $ARGUMENTS

